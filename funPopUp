//will create some functions as utils


//binds a variable around a tag and it's content 
function wrapCont(tag){
		return function (contenuto){
    var apri = "<" + tag + ">";
    var chiudi = "</" + tag +">";
    return(apri + contenuto + chiudi );
 }}
